@* @model IEnumerable<Question>

@{ ViewData["Title"] = "Exam Me AI" }

<h1>@ViewData["Title"]</h1>
<form asp-controller="Exam" asp-action="Index" method="get">
    <select asp-for="QuestionText" asp-items="Model.Question">
        <option value="">Choose Question !!</option>
    </select>
</form> *@

@model IEnumerable<Question>

@{
    ViewData["Title"] = "Exam - Index";
}

<h2>Choose a Question</h2>

<form asp-action="Index" method="post">
    <select name="selectedQuestionId" class="form-control">
        @foreach (var q in Model)
        {
            <option value="@q.QuestionId">@q.QuestionText</option>
        }
    </select>

    <button type="submit" class="btn btn-primary">Submit</button>
</form>
